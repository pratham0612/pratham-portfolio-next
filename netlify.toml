[build]
  command = "npm ci && npm install --force --legacy-peer-deps"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "16"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
